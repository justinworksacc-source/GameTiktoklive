export const metadata = {
  title: "Gift Dash — TikTok LIVE Race",
  description: "TikTok LIVE gifts boost racers in real time."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
